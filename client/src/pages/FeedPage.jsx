import { useEffect,useState } from 'react';
import Avatar from '../components/Avatar';
import { fallbackPosts,trendingStories } from '../data/demo';
import { api } from '../services/api';
export default function FeedPage({onProfile}){ const [posts,setPosts]=useState(fallbackPosts); const [newPost,setNewPost]=useState(''); useEffect(()=>{api('/posts').then(data=>setPosts(data.posts.length?data.posts:fallbackPosts)).catch(()=>setPosts(fallbackPosts));},[]); const createPost=async(event)=>{ event.preventDefault(); if(!newPost.trim()) return; const data=await api('/posts',{method:'POST',body:JSON.stringify({text:newPost,place:'Current city'})}); setPosts(current=>[data.post,...current]); setNewPost(''); }; return <section className="app-page feed-page"><div className="page-header"><div><p className="eyebrow">Page 1</p><h1>All users posts</h1></div><input className="search-bar" placeholder="Trending in London"/></div><div className="stories-row">{trendingStories.map(story=><div className="story-card" key={story}><span>{story[0]}</span><strong>{story}</strong><small>trender</small></div>)}</div><form className="post-box" onSubmit={createPost}><input value={newPost} onChange={event=>setNewPost(event.target.value)} placeholder="Aaj kya share karna hai?"/><button type="submit">Post</button></form><div className="post-list">{posts.map(post=><article className="post-card" key={post._id}><button className="profile-link" type="button" onClick={()=>onProfile(post.author)}><Avatar user={post.author}/><span><strong>{post.author?.name}</strong><small>{post.place||post.author?.location?.district}</small></span></button><p>{post.text}</p><div className="post-meta"><span>{post.likes||0} likes</span><span>{post.author?.location?.country}</span></div></article>)}</div></section>; }
