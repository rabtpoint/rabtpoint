import Avatar from './Avatar';
export default function ProfileModal({user,onClose}){ if(!user) return null; return <div className="modal-backdrop" onClick={onClose}><article className="profile-modal" onClick={e=>e.stopPropagation()}><button className="icon-button close-button" type="button" onClick={onClose}>x</button><Avatar user={user} size="lg"/><h2>{user.name}</h2><p>{user.bio||'RabtPoint user'}</p><div className="profile-grid"><span>Country</span><strong>{user.location?.country}</strong><span>State</span><strong>{user.location?.state}</strong><span>District</span><strong>{user.location?.district}</strong><span>Latitude</span><strong>{user.location?.latitude}</strong><span>Longitude</span><strong>{user.location?.longitude}</strong></div></article></div>; }
